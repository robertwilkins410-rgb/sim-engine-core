{
  "name": "sim-engine-core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Modular career simulation engine. Theme-agnostic core with pluggable theme modules.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
